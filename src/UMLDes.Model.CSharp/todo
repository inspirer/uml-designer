модель - это дерево, в котором любой элемент находится по Qualified имени
	(включая имя проекта) во время update подключаются все типы сохраняя
	текущее дерево потом инициализируется резолвер

Состояние модели:
	построены все типы из Source Project-ов, к ним прицеплены using-и
	подгружены MSIL проекты (не обновляются)
	настроены referenc-ы между проектами


Сделано:
   убрать конструкторы из Node-ов
   разнести ноды
   написать визуализатор нодов/модели
   привести парсер в порядок, обработка приведения типов
   ноды - internal, модель - public
   persistence модели в Xml
   оформить модель в отдельную сборку UMLDes.Model.dll

Нужно:
   обработка комментов
   добавить восстановление после ошибок
   написать конвертер ноды -> модель 

   анализ кода, выявление агрегирования, ассоциаций и т.д. (эвристики)
   сохранение связей в .cs файлах

Визуализация:
   подгружаемые диаграммы UMLDes.Views.dll, UMLDes.StaticView.dll
   таскание namespace-ов, enum-ов и др. типов на диаграммы

